# Activity Summary

Answer each question in full sentences. Each answer starts as a placeholder marker. Replace
every marker with your own writing, and delete the marker itself. The minimum word count for
this document is `100` words.

> Paste the report your program printed for the numbers `17` and `5`, entered in whatever
> format your prompt asks for. Put it inside a fenced code block, which is three backticks on
> a line of their own, then your output, then three more backticks. Preview this file before
> you push it and confirm the block renders as a block.

TODO

> Three of your report lines print the same kind of text three different ways: with commas,
> by joining strings with `+` and `str()`, and with an f-string. Name one thing that was
> easier and one thing that was harder about the version that joined strings.

TODO

> Compare the `/` line and the `%` line for the pair `17` and `5`. What does each operator
> hand back, and why do the two results look so different even though they start from the
> same two numbers?

TODO
