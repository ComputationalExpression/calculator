[project]
name = "activity-02-calculation-report"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "gatorgrade",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = [
    "src"
]
testpaths = [
    "tests"
]
filterwarnings = [
    "ignore::pytest.PytestAssertRewriteWarning",
]
